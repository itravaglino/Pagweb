# Cursor Cloud

- `npm install`
- `npm test`
- `npm run dev` sirve el emulador en `http://localhost:3000`
- Hace falta Node 20+
- La UI funciona sin GPU (modo demo). Con Ollama, Gemma corre en la GPU local. WebGPU en Chrome queda como respaldo.
