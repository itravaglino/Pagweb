# Cursor Cloud

- `npm install`
- `npm test`
- `npm run dev` sirve el emulador en `http://localhost:3000`
- Hace falta Node 20+
- La UI funciona sin WebGPU (modo demo). Gemma on-device requiere Chrome con WebGPU.
