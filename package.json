{
  "name": "pagweb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Studio customizable de avances + demo Mejor Día (Fitbit + NVIDIA NIM)",
  "scripts": {
    "dev": "node server/index.js",
    "build": "node scripts/seed-db.js && vite build --config client/vite.config.js && mkdir -p dist/data && cp -f data/pagweb.json dist/data/",
    "build:pages": "npm run build && rm -rf docs && cp -r dist docs && touch docs/.nojekyll",
    "start": "NODE_ENV=production node server/index.js",
    "test": "node --test tests/*.test.js"
    "preview": "npm run build && npm start"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "motion": "^13.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^6.3.5"
  }
}
